USE appks;

//"INSERT INTO Files(dataset_ID, ID, Name, Creation_date, Amount_of_bytes, Blob_data) VALUES (?,?,?,?,?,?)"

//"SELECT dataset_ID, ID, Name, Creation_date, Amount_of_bytes, Blob_data FROM Files WHERE dataset_ID = ?"